using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace DW.Worlds {
    public enum QuadPlane { XPlane, YPlane, ZPlane };
    public enum Position { Back, Front };
    public enum HeightCalculation { relative, absolute };
}
